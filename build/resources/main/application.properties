server.port=8080
spring.application.name=nutriwarrior

# JPA comum
spring.jpa.show=sql=true
spring.jpa.hibrernate.ddl-auto-auto=nome

# Flyway comum
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Profile Padrão
spring.profiles.active=dev


